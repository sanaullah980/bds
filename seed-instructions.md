## Seed instructions

To create a demo user and business quickly, after setting up your .env and running the migrations run:

npm run seed

This will create a user `demo@shop.test` with password `password123` and a business `Demo Shop`.

You can then sign in with the credentials.
